import React from "react";

const page = () => {
    return (
        <main className="w-full min-h-screen pt-[5rem] flex items-center justify-center">
            <h1>Sports Page</h1>
        </main>
    );
};

export default page;
